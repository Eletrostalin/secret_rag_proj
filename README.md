Обновление 19.07
Что сделано: 
1. Pаменил ембддер с длиной вектора потестировал разные, выбор пал на intfloat/multilingual-e5-large. Находится в embedder.py
2. Заменил 4o mini на другие модели, в результате тестов теперь это выгляит так:
    •	Классификация запроса — gpt-4o-mini
	•	Переформулировка и расширение запроса — gpt-4o
	•	Генерация ответа — gpt-4.1 
	•	Фильтрация и rerank чанков — gpt-4.1
3. Теперь если запрос просит процетировать конкретный праграф ищем прямо по номеру. Реализация в новом модуле Quote_handler.py
4. Векторный поиск теперь ранжирует результы по косинусной схожести. Потестировал и выбрал схожесть 0.8. Все что больше подпадает 
в top n. Если таких не обнаружено делаем фоллбек на 10 ближайших по смыслу чанков
5. В общий пайплан после bm25 и векторного поиска добавил реранк чанков моделью перед генерацией общего запроса

Приложение на моей машине запускается долго. В начале минуты две надо подожать инициализации бд. 
Появится лог PostgreSQL init process complete; ready for start up.
Потом когда бд поднимется, парсинг дока тоже займет минут 5- 7, потому что вектора большие, можно бахнуть чая в это время.
В логах ✅ Embeddings saved successfully. и ✅ BM25 indexing complete.

Команда для запуска приложеия с nginx и ngrok

docker-compose -f docker-compose_remote.yml up --build
